spring:
  application:
    name: payment
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      username: user
      password: zero-one
  profiles:
    active: local
#    active: prod
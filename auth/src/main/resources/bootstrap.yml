spring:
  application:
    name: auth
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: zero-one
      username: user
  profiles:
    #    active: local
    active: dev
